# 2026-09-12

<<<<<<< HEAD
## 新增：未保存内容自动备份与恢复（@scene#15 日常开发）

实现 F HamLog 的崩溃/异常关闭恢复机制，新增模块 `backup.py`：

- 备份文件：`file/project_backup.fhl`（沿用项目加密 key）与 `file/batch_backup.fh`（始终明文）。
- 语义：备份文件非空=有可恢复内容；空文件(0字节)=无需恢复。空列表写入即置空。
- 打开项目/批量记录时：把当前(已保存)内容写入备份作基线；正常保存(`save`/`osave`/`esave`/`append_to_project`/`save_all`)后清空备份并更新快照。
- 周期定时器(1.5s)：存在未保存更改(当前 `file`/表格 与快照不一致)时写入备份，覆盖崩溃场景。
- 关闭守卫 `install_close_guard`：无未保存→直接关并清备份；有未保存→弹「保存/不保存/取消」。保存=落盘+清备份；不保存=保留含未保存内容的备份(供下次恢复)；取消=保持打开。
- `main.py` 启动(`QTimer.singleShot(0)`)读取两个备份，非空则弹「恢复/忽略」；恢复则打开对应窗口并回填(`batch_project.main(preset_records=...)`)，随后清空备份避免重复提示。
- 验证：venv(PySide6-Essentials) offscreen 冒烟通过——backup.py 单测、关闭守卫三分支、批量窗口基线写入/干净关闭清空/恢复回填全部 PASS。注意 `project.py` 主窗口 offscreen 硬崩溃（已知环境限制），故项目窗口 GUI 路径未能 headless 验证，逻辑与批量窗口同源。

## 踩坑
- `install_close_guard` 在 `backup` 模块内，调用处须用 `backup.install_close_guard(...)`（初版漏写 `backup.` 导致 NameError）。
- 沙箱 bash 的 PATH 损坏(`dirname`/`ls`/`grep`/`cat`/`rm` 均不可用)，改用绝对路径 venv python(`Scripts/python.exe`) + 绝对路径重定向 + 文件落盘后 Read 查看；PowerShell 输出被吞。

## 调整：恢复后默认视为「有未保存的更改」
- 需求：启动恢复进来的内容应默认带未保存更改，关闭时再次提示保存（且「不保存」可再次恢复）。
- 实现：`project.main` 与 `batch_project.main` 新增 `recovered=False` 形参；当 `recovered=True` 时，把基线快照 `_bk_state['last']` 置为 `''`（永不等于真实 `json.dumps` 的哨兵），使 `_bk_is_dirty()` 恒为 True。
- `main.py`：恢复到窗口的路径传 `recovered=True`；并改为「仅『忽略』时清空备份，『恢复』时不清空」——交由恢复窗口接管备份（保持脏标记、可再次恢复）。

## 调整：恢复的「保存」直接存回恢复文件，不弹「新建文件」
- 用户纠正：恢复后点「保存」不应弹「新建文件」另存为，而应「保存恢复的文件」。
- `project._do_save`：新增 `recovered` 分支——`save_path` 仍为空时，弹标题为「保存恢复的文件」、默认名「恢复的项目.fhl」的另存为对话框（区别于「新建文件」）；保存后把 `save_path` 指向所存文件(`nonlocal`)、清空临时恢复备份 `file/project_backup.fhl`、并更新快照。之后按正常逻辑判断未保存更改（已落盘则不再脏）。
- 关键坑：曾试过把 save_path 直接设为 `file/project_backup.fhl` 并在清空时跳过，但会导致该文件永远非空→每次启动都误报恢复。故改为「另存为真实文件 + 清空临时备份」的干净方案。
- 注意 `project.main` 在 offscreen 下硬崩溃（环境限制），此分支无法 headless 验证，仅 py_compile + 导入 + 代码审查通过；本机需手动走「恢复→保存→重开不误报」。

## 调整：关闭提示改为显式中文字符
- `backup.py` 的 `install_close_guard` 原先用 `QMessageBox.Save|Discard|Cancel` 标准按钮，在英文 Windows 上会显示 Save/Discard/Cancel。改为 `msg.addButton('保存', AcceptRole)` / `msg.addButton('不保存', DestructiveRole)` / `msg.addButton('取消', RejectRole)`，确保中文；分支逻辑不变（按 `clickedButton()` 判定）。`main.py` 启动恢复提示本来已是中文(`恢复`/`忽略`)。

## 调整：恢复项目“新建文件”弹窗修复 + 统一“保存恢复的文件”
- 用户最终确认：**批量逻辑完全不动**，只在**项目**里把“新建文件”改成“保存恢复的文件”语义。
- 根因：恢复项目 `main()` 传入 `save_path=''`，而 `project.py` 第665行 `if save_path == ''` 会在**打开时**立刻弹“新建文件”逼用户选路径；工具栏“保存”(`save()`)在 `save_path==''` 时走 `osave()` 弹“另存为文件”。两处都冒出“新建文件/另存为”。
- 修复（仅 project.py，batch 未动）：
  1. 第665行启动提示加 `and not recovered` 守卫：恢复项目打开时**不弹**“新建文件”，窗口标题暂显「恢复的项目」；
  2. 抽 `_save_recovered_as()`：弹「保存恢复的文件」对话框(默认名`恢复的项目.fhl`)→写入用户所选真实路径→`save_path=sp`→`clear_backup`→`_bk_snapshot()`(消除未保存标记)→更新标题；
  3. `save()` 在 `save_path==''` 且 `recovered` 时改调 `_save_recovered_as()`（不再调 `osave()`）；
  4. 关闭守卫 `_do_save` 也复用 `_save_recovered_as()`（去掉重复对话框代码）。
- 语义：恢复打开=脏(关闭会提示)；点“保存恢复的文件”=写入路径+清标记(不再脏)；之后再次编辑=定时器重新标记脏。普通新建项目仍弹“新建文件/另存为”(行为不变)。
- 验证：四文件 py_compile 通过；逻辑仿真(复刻快照/脏标记数学)5项 PASS；`project.main` offscreen 硬崩溃属环境限制仍无法 headless 跑通，本机需手动走「恢复→保存→重开不误报」「恢复→改一条→关闭再提示」两路径。

## 新增：卫星选择对话框「未搜索时已选卫星置顶」（satellite_window.py）
- 需求：`SatelliteSelectDialog` 在**未搜索**（搜索框为空）时，始终把已勾选的卫星排到列表顶部。
- 实现（`satellite_window.py`）：
  1. `_reorder_pin_selected()`：按“已选在前(保持各自原相对顺序) + 未选在后”物理重排 `QListWidget` 的 item，并同步 `self._row_names`（供 `_filter` 按行隐藏用）。顺序未变则直接 return（避免闪烁）。
  2. `_filter()` 末尾：`if not self._keys: self._reorder_pin_selected()`——仅未搜索时置顶；搜索中不重排（只显示匹配项）。
  3. 新增 `_on_item_changed`：连接 `list_widget.itemChanged`，未搜索时勾选状态一变就重新置顶（新勾选自动跳到顶部）。
  4. 重排守卫 `self._reordering` 防 `itemChanged` 递归。
- **性能要点**：取出全部 item 时用尾部 `takeItem(count-1)`（每次 O(1)）而不是 `takeItem(0)`（每次 O(N)），再追加式 `insertItem` 插回，整体 O(N)，避免数千颗卫星时 O(N²) 卡顿。item 对象始终被 `self.items` 引用不会被回收。
- 该对话框被 `satellite_window`（过境）与 `mutual_window`（通联预测）共用，两处同时生效。
- 验证（venv offscreen）：新写 `__pin_selected_smoke.py` 6 项全 PASS（初始置顶/取消取消掉出置顶组/新勾选加入/搜索仅显示匹配且不重排/清空搜索重新置顶/get_selected 正确）；原 `__rowhidden_smoke.py` 无回归(RESULT: OK)。两测试脚本保留（`__*_smoke.py` 与既有探针惯例一致）。
=======
## GitHub Action 自动打包
- 重写 `.github/workflows/main.yml`：在 `windows-latest` 用 Nuitka 构建独立 exe（与 `打包.txt` 参数一致：含 skyfield/numpy 包 + `file/` 数据目录、输出名 `F HamLog 2`、禁用控制台、UAC 管理员、pyside6 插件）。
- 触发：`workflow_dispatch` 手动运行（可覆盖 Python 版本与 exe 名）。**不自动构建**——用户要求推送/合并不触发，已移除 `push` 触发块。
- 依赖按 `第三方模块.txt` 安装；pip 缓存加速；产物 `main.dist` 经 `upload-artifact@v4` 上传为 `F-HamLog-dist`。
- 注意：`file/` 内含 `python-3.13.11-amd64.exe`（~30MB）与 `main.fhl`，当前随 `--include-data-dir=file=file/` 一并打进分发包（与本地一致）；若想瘦身可改为按需拷贝数据文件并排除安装包。
- CI 构建报错修复：Nuitka 在 Windows standalone 模式下需 Dependency Walker，非交互 CI 默认回 `no` 导致 FATAL。已在 nuitka 命令加 `--assume-yes-for-downloads`（等价环境变量 `NUITKA_ASSUME_YES_FOR_DOWNLOADS=1`），让其自动下载。
- 另：用户提到「关闭时提示中文」，尚未明确作用到哪个窗口（主启动窗口 main.py:101 的 QMainWindow / 日志窗口 project.main / 全部），待确认后再实现关闭确认对话框。
>>>>>>> origin/workbuddy/develop-dd5fd3c1
