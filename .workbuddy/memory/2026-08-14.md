# 2026-08-14

- project.py 新增「查找替换」功能（按字段查找并替换）。入口：功能菜单「查找替换」(Ctrl+H)，紧邻「搜索」。实现 `find_replace()`：选择字段、查找文本、替换为文本、匹配方式(包含/完全匹配)、范围(全部匹配记录/仅选中的行)；实时预览影响记录数与替换处数；替换前 `snapshot_before()` 落撤销点，确认对话框后批量改写 `file` 并 `table_update()`。匹配不区分大小写（与 `research_call` 一致），包含匹配用 `re.sub(..., flags=IGNORECASE)` 且 `re.escape` 转义查找文本；完全匹配用 `val.lower()==q.lower()`。已通过 `py_compile` 与 6 项逻辑测试（含大小写不敏感、值内多处、选中行范围、无匹配、特殊字符转义）。

- 「搜索」(`research_call`) 同步补齐「范围」下拉，与「查找替换」一致：选项 `全部匹配记录` / `仅选中的行`。选 `仅选中的行` 时仅保留主页面勾选（或当前选中）的行，依赖 `get_selected_row_indexes()`。对话框高度由 150 调到 180 容纳新增行。已 `py_compile` 通过，并用独立脚本验证过滤逻辑（全部命中 [0,2]；仅选中的行选中[1,2,3]→[2]；未选中→[]）。

- 「搜索结果」窗口(`research_window`) 选择逻辑/快捷键对齐主窗口 project.py：新增「选择」菜单（全选 Ctrl+A / 反选 Ctrl+I / 取消选择 Ctrl+D），并补 `_search_invert` 反选函数；右键菜单由「全选/取消全选」改为「全选/反选/取消选择」；**移除顶部的「全选」「取消全选」按钮**，仅保留提示文字。复选框仍实时同步到主页面选择框。已 `py_compile` 通过。

- 统计图改造：(1)「统计图表」统一改名为「统计图」（菜单项 + 对话框标题）；(2) `show_statistics` 增加 `records=None` 参数与「范围」下拉（全部匹配记录 / 仅选中的行），与主窗口「搜索」一致——`records` 为 None 时按范围取主数据，否则只统计传入的记录；(3) 搜索结果窗口新增「统计图」菜单（仅统计 matches 搜索结果）与「导出」菜单（FHL/ADI/表格，仅导出 matches）。导入未实现（搜索结果为只读子集，导入语义不符，且用户澄清只统计/导出）。已 `py_compile` 通过。

- 搜索结果窗口(research_window)菜单栏再调整：(1)「统计图」由顶级菜单改为归入「功能」菜单（与主窗口一致）；(2) 原「导出」菜单改名为「导入/导出」，结构/标签与主窗口「导入/导出」完全一致——导入项复用主窗口 `import_from_ADI/input_fhl/input_HAM_tolls_`（导入进主项目），导出项（`导出ADI文件`/`导出为表格`/`导出选中的日志`子菜单 FHL/ADI/表格）改为仅导出搜索结果 matches（复用 `_export_search_*`）。已 `py_compile` 通过。

- 主窗口「文件」菜单新增「打开主页面」（快捷键 Ctrl+Shift+M）：点击后用 `subprocess.Popen([sys.executable, <project_dir>/main.py])` 启动 main.py 启动器窗口（即程序入口/主页面）。已 `py_compile` 通过。

- 呼号输入统一大写（新增 `call_upper.py` 统一方案）：`UpperCallDelegate`（表格呼号行单元格编辑实时转大写）+ `connect_callsign_upper(edit, field_getter)`（QLineEdit，仅字段为 m_call/o_call 时转大写，设置「我的呼号」用恒定 lambda）。接入：① project.main() 打开项目时遍历 file 把每条 m_call/o_call 转大写；② project.py 的 new()/project_others() 表格行2(己方)/行3(对方) 挂委托并保留引用；③ research_call 搜索关键词、④ find_replace 查找/替换框按字段联动；⑤ set.py「我的呼号」固定转大写；⑥ batch_project.py 模板表/翻页表行2/3 挂委托且 app_list['m_call'] 取自设置即转大写。已 `py_compile` 通过 + 离屏冒烟（固定字段/字段联动/委托编辑器/光标保持 4 项断言全过）。委托引用须保留（`table._upper_call_delegate = delegate`）。
