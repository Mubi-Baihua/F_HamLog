# 2026-08-18 工作日志

## 修复：导入/导出对话框重复 (project.py)
- Bug：导入/导出文件时弹出多个对话框。
- 根因：
  1. 导出 ADI 主窗口 `output_adi` / `output_selected_adi` / 搜索窗口 `_export_search_adi` 在 `project.py` 重复弹"导出成功"，而 `output_adi.py` 内部已弹过（带路径）→ 出现两个成功框。
  2. 三个导入函数（`input_HAM_tolls_` / `import_from_ADI` / `input_fhl`）选完文件后又多弹"应用导入吗？"确认框，多余。
- 修复：删除 `project.py` 中 ADI 导出三处冗余的"导出成功"提示（保留 `output_adi.py` 内唯一带路径的）；删除三处导入的"应用导入吗？"确认。Excel/FHL 导出在 `project.py` 仍保留各自唯一成功提示（其模块无内部提示）。
- 验证：py_compile 通过；offscreen 行为测试确认导入=1 文件框(0 确认)、导出 ADI=1 保存框+1 成功提示。
- 注：导入时若检测到重复记录，仍会弹"覆盖/跳过"选择框（必要交互，非冗余），属正常。

## 修复：点击"导出为表格"卡死 (output_excel.py)
- Bug：点击"导出为表格"/"导出选中的日志为表格"后界面立刻卡死（无响应）。
- 根因：`output_excel.main` 在 Qt GUI 线程同步执行，且首行 `import pandas`+`import numpy` —— 冷导入 pandas+numpy 约 24s、热导入约 1.2s，全程阻塞事件循环 → 整个程序冻结（"卡死"）。导出 50 行实测 26s。
- 修复：重写 `output_excel.py`，去掉 pandas/numpy，改用 openpyxl 直接写 xlsx（openpyxl 本就是项目依赖，input_HAM_tolls.py 已用）。保留中文表头翻译字典、保存对话框、列宽自适应、`record` 字段不导出、成功/失败提示契约（返回 True/False，project.py 的成功框不变）。
- 验证：重写后冷导出 50 行 1.69s、热导出 200 行 1.60s；17 列、record 已排除；空列表/非字典条目/取消 均正常；py_compile + import 通过。
- 附带收益：pandas 现已无代码引用（仅 numpy 仍被 satellite_pred.py 使用），Nuitka 打包可不再捆绑 pandas，减小体积、加快启动。

## 统一：Excel 导出成功提示格式 (output_excel.py + project.py)
- 需求：Excel 导出成功提示与 ADI 一致，统一为"已导出 n 条记录到：\n{路径}"。
- 改动：`output_excel.main` 在 `wb.save` 后自带 `QMessageBox.information(parent_window,"导出成功", f"已导出 {len(file or [])} 条记录到：\n{save_path}")`（与 output_adi.main 同款）；删除 project.py 中 3 处 Excel 导出的冗余"导出成功！"提示（主窗口 output_excel、output_selected_excel、搜索窗口 _export_search_excel）。FHL 导出（output_selected_fhl / _export_search_fhl）的"导出成功！"提示保持不变。
- 验证：py_compile 通过；offscreen 行为测试（拦截对话框计数+读回 xlsx）确认 Excel 导出仅弹 1 个提示且内容为"已导出 2 条记录到：\n{临时路径}"，文件行数正确，`ALL_ASSERTIONS_PASSED`。

## 需求：FHL 导出统一对话框 + Excel 补全所有字段
- 需求1：将"导出选中项目为FHL"（output_selected_fhl）与搜索结果导出FHL（_export_search_fhl）的成功提示统一为"已导出 n 条记录到：\n{路径}"。已在 project.py 两处将 `QMessageBox.information(...,"导出成功","导出成功！")` 改为带数量+路径的统一格式。注意：保存/另存项目文件本身的"保存成功/另存成功"（~490-513、1428）不属于导出，未改动。
- 需求2：优化 Excel 导出，加入所有日志条目字段。原 output_excel 硬编码 17 字段，漏掉 freq_rx/prop_mode/sat_name 等。改为动态收集数据中出现的全部 dict 键（排除 record），已知字段用中文列名并按定义顺序排列，未知字段自动按出现顺序追加（表头用原始键名）。translation_dict 补充 freq_rx→下行频率、prop_mode→传播模式、sat_name→卫星名称。
- 验证：py_compile 通过；offscreen 行为测试（含 freq_rx/prop_mode/sat_name 及未知字段 my_extra 的样例）确认导出 21 列、所有已知+未知字段均出现、record 被排除、提示统一，`ALL_ASSERTIONS_PASSED`。
