2026-08-12
- 移除 `load_sat_radio_dict()` 回退到 `SATE_BANDS` 的逻辑，文件不存在时返回空 dict
- 卫星转发器数据表（sat_radio_dict.txt）完全依赖用户维护
- 从之前版本导入数据支持 `sat_radio_dict.txt` 和 `tqsl_dict.txt` 迁移，每个文件存在时追加到导入清单
- 修复 TLE 自动更新时间戳写入：① `start()` 立即调用 `_tick()`，解决首次要等 1 小时才触发、导致 sat_last_update 永远不写入的问题；② 加固 `_save_last_fetch`，读取设置失败时不再清空用户设置；③ 成功分支加确认日志
