# 2026-08-29

## 统一“使用记事本编辑”文字提示与按钮文本
场景 @scene#15 日常开发。需求：TQSL映射、转发器设置、管理标记点三处配置编辑入口，文字提示与按钮文本统一为“使用记事本编辑”。

改动文件：
- `satellite_map_window.py`：
  - `MarkerManageDialog` 的“打开标记点TXT”按钮 → 改为 `使用记事本编辑`（与 TQSL/转发器对话框内的记事本按钮一致，消除唯一不一致项）。
  - 顶部说明 Label 改为 `标记点配置统一使用记事本编辑（file/sat_map_markers.txt）。`
  - 地图窗口工具栏“标记点管理”按钮补 ToolTip：`使用记事本编辑地图标记点（名称/网格/纬度/经度/颜色）file/sat_map_markers.txt`。
- `satellite_window.py`：
  - 工具栏“编辑转发器”/“编辑TQSL映射”按钮 ToolTip 改为以“使用记事本编辑”起头并点明配置文件名。
  - TQSL 映射缺失提醒消息改为「可点击工具栏“编辑TQSL映射”使用记事本补充该卫星的映射。」；保存注释里的“TQSL映射”→“编辑TQSL映射”与按钮名一致。
- `mutual_window.py`：TQSL 映射补充提醒消息补“使用记事本”。

验证：三个文件 `py_compile` 通过；改动均为纯字符串（按钮文本/提示），未动逻辑。
