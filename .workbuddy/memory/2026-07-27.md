# 2026-07-27 工作日志

## 新增功能：卫星星历自动更新 + 卫星字典 GUI 编辑

为“日常开发”场景新增两个功能：

1. **星历自动定时更新**
   - `set.py`：设置窗口增加「卫星星历自动更新」复选框 + 「更新间隔(小时)」SpinBox(1–168)，写入 `m_xml.txt` 的 `sat_auto_update` / `sat_update_hours`。
   - 新建 `satellite_auto_update.py`：`AutoTleUpdater`（QTimer 每小时巡检 `should_update_now()` + 后台 `_FetchThread` 调 `fetch_amateur_tle(force=True)` 刷 `file/amateur.tle`），成功记录 `sat_last_update`；开关/间隔改后无需重启即生效。
   - `main.py`：启动时 `AutoTleUpdater(window).start()` 常驻（app 级生命周期）。

2. **卫星界面用 PySide6 编辑 sat_radio_dict / tqsl_dict**
   - `satellite_window.py`：新增通用 `DictEditorDialog`（保留 `#` 注释、多值按 `,` 拆分、单列值用 `value_delimiter=None`）；工具栏加「编辑转发器」(`SAT_RADIO_DICT_PATH`) 与「TQSL映射」(`TQSL_DICT_PATH`) 两个按钮，保存后重跑预测；另加「星历自动更新」复选框实时开关设置。

验证：4 个文件 `py_compile` 通过；沙箱桩 PySide6 冒烟测试通过（`load_sat_radio_dict`/`load_tqsl_dict`/`lookup_transponder`/`should_update_now`/`AutoTleUpdater` 构造）。GUI 真实运行需在含 PySide6+skyfield 的环境验证。

## 布局优化：卫星窗口工具栏重新排布
- `satellite_window.py`：原工具栏所有控件（11+ 个）挤在单行过于拥挤。改为两行分组：
  - 上组「数据与设置」：刷新TLE / 观测站设置 / 编辑转发器 / TQSL映射 / 导入转发器 / [右侧] 星历自动更新☑
  - 分隔线（QFrame.HLine）
  - 下组「预测参数」：开始时间 / 预测时长 / 最小仰角 / 范围 / 选择卫星…
  - 导入 QGroupBox；移除旧 `tool` 布局变量，编译通过且无遗留引用。

## 界面优化：main.py 启动器窗口
- 重写 `central_widget` 布局：统一按钮尺寸/圆角样式；主操作「通联日志」用强调色(#2f6fed)加粗白字、Enter 默认触发；次要操作（批量记录/QRZ主页/卫星过境/新建项目/打开项目）改为 220 宽两列 QGridLayout；各按钮加 QStyle 标准图标；标题 18pt 加灰色副标题；去掉临时空格占位 QLabel；底部分隔线 + 「设置」。
- 窗口固定尺寸 500×325 → 500×380，主布局间距 10、内边距(24,18,24,16)。
- 编译通过，无 `row_widget/text_label/h_layout` 残留引用。

## 新增功能：project.py 主表格「选择」菜单（全选/反选/取消选择）
- 在菜单栏「导入/导出」与「功能」之间新增顶层「选择」菜单。
- 三个 QAction：`全选`(Ctrl+A)、`反选`(Ctrl+I)、`取消选择`(Ctrl+D)，操作主表格第0列“选择”复选框。
- 辅助函数 `set_all_rows_checked(checked)` / `invert_rows_checked()`：遍历 `table.cellWidget(row,0).findChild(QCheckBox)` 设置/取反勾选，与 `get_selected_records()` 读取逻辑一致。
- `py_compile` 通过。GUI 真实交互需在含 PySide6 的环境验证。

## 新增功能：project.py 跨窗口复制/粘贴 + 撤销/重做
- 模块顶部新增常量 `COPY_FIELDS`（20 个字段顺序）与 `FIELD_LABELS`（字段->中文表头）；新增 `import copy`。
- **撤销/重做**：`main` 内 `undo_stack`/`redo_stack`；`snapshot_before()` 在每次修改 `file` 前深拷贝入栈（已接入 new/project_others编辑与删除/三个导入/quick_log/run_pack/粘贴 共 8 处）；`undo()`/`redo()` 弹栈恢复并 `table_update()`。菜单栏新增顶层「编辑」菜单：撤销(Ctrl+Z)/重做(Ctrl+Y)/复制(Ctrl+C)/粘贴(Ctrl+V)。
- **复制/粘贴（跨窗口/跨软件）**：`copy_records_to_clipboard(records)` 以 Tab 分隔、英文字段名表头写入系统剪贴板；`copy_from_main()` 复制“选择”列勾选行（回退到表格选中行）；`paste_to_main()` 解析剪贴板（兼容英文键名或中文表头、部分列、CRLF），`snapshot_before()` 后 `file.extend` 追加。主表格右键菜单含 全选/反选/取消选择/复制/粘贴/撤销/重做。
- **搜索结果窗口**：`table_r` 右键菜单新增「复制选中行」（复制底层 `file[orig_index]` 记录，可粘贴到主窗口或 Excel），以及 全选/取消全选。
- 注意：project_others 的「保存」改为先整体校验、通过后才 `snapshot_before()` 再写入，避免校验失败产生空撤销点。
- `py_compile` 通过；复制/粘贴 TSV 解析逻辑已用独立脚本单测（英文往返、中文表头、部分列、非法表头均正确）。GUI 真实交互需在含 PySide6 的环境验证。
- **删除“常用FM卫星”选项**：用户于 21:52 要求删除卫星选择对话框中的「仅FM常用」按钮（即其口中的“常用FM卫星”选项）。已从 `satellite_window.py` 移除 `SatelliteSelectDialog` 顶栏的 `仅FM常用` 按钮（line 148 处 `self._mk_btn('仅FM常用', self._select_fm)`）及其对应的 `_select_fm()` 方法（死代码一并清除）。`py_compile` 通过，无残留引用。卫星选择对话框现仅剩 全选/全不选。
- **TQSL 映射失败时提示**：用户于 21:55 要求“当无法进行TQSL映射的时候提示”。在 `satellite_pred.py` 新增 `has_tqsl_mapping(name)`（判断显示名是否在 `tqsl_dict.txt` 中存在映射）；在 `satellite_window.py` 的 `log_row()`（点「记录」按钮）里，若 `preset` 存在且该卫星无 TQSL 映射，弹 `QMessageBox.warning` 提醒将以原始名称保存、可能不被 LoTW/TQSL 识别，并提示用工具栏「TQSL映射」补充；提示后仍继续记录。两文件 `py_compile` 通过。
- **卫星时间精确到秒 + 从 0° 起算**：用户于 22:47 要求。改动：① `satellite_pred.py` 的 `predict_passes` 将 `find_events` 的 `altitude_degrees` 由 `min_elev` 改为 `0.0`，AOS/LOS 改为从 0° 地平线起算（秒级精度），原“最小仰角”仅作为过滤（保留 `max_elevation >= min_elevation_deg` 的过境）；更新 docstring。② `satellite_window.py`：`_utc_to_local_str` 显示格式 `%m-%d %H:%M` → `%m-%d %H:%M:%S`；`_build_row` 预填 `time` 由 `%H:%M` → `%H:%M:%S`（卫星记录入库带秒）。③ `project.py` 放宽“更多信息”编辑里时间校验正则 `^\d{2}:\d{2}$` → `^\d{2}:\d{2}(:\d{2})?$`，允许 HH:MM:SS 编辑。验证：`py_compile` 通过；用 venv(skyfield) 冒烟测试 confirm 0° AOS 带秒、过滤正确（min_elev=0→8 / 10→4 / 70→0 次）。ADIF 导出 `_local_to_utc` 对 `HH:MM:SS` 取前 HH:MM 截断，不受影响。
- **修复卫星时间「比其他应用早1小时」**：用户于 22:55 反馈。根因：`satellite_window.py` 的 `_observer_tz(lon)=timezone(timedelta(hours=round(lon/15)))` 按经度÷15取整推算时区，与中国统一 UTC+8 不符；当 `m_lon` 落在 +9 档（如135°E）即显示比北京时间早1小时（已用 skyfield `utc_datetime()`（返回 aware UTC）实证：旧逻辑09:28 vs 系统本地08:28）。修复：删除 `_observer_tz`，改用 `LOCAL_TZ = datetime.datetime.now().astimezone().tzinfo`（系统本地时区），统一用于预测列表显示、记录预填、起始时间编辑框默认值、起始时间→UTC 解析；物理轨道解算仍用真实经纬度不受影响。更新了相关 toolTip/注释。`py_compile` 通过，无残留引用。
