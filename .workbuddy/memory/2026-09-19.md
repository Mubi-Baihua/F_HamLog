# 2026-09-19 工作日志

## 开放多人日志默认端口改为 8000

用户需求：「使用客户端创建的房间，端口也默认为 8000」。原 `project._open_lan()` 用 `port=0` 随机分配，
现改为以 **8000** 为默认端口（与「加入多人日志」对话框的 `port_e = QLineEdit('8000')` 默认值一致，
便于同局域网按默认值直接对接）。

### 改动
- `project.py`：
  - 新增模块级常量 `DEFAULT_ROOM_PORT = 8000`（附注释说明与加入对话框默认值保持一致）。
  - `_open_lan()` 改为 `LogServer(..., port=DEFAULT_ROOM_PORT, ...)`；若 `srv.port_status == 'occupied'`
    则重建为 `port=0` 自动分配（**优雅回退**，避免 8000 被占时直接开放失败）。
- `remote_server.py`：
  - `LogServer.__init__` 新增实例属性 `port_status`（`'ok' | 'occupied'`）：构造期对显式端口做一次
    占用探测；`port=0`（自动分配）恒为 `'ok'` 不探测。
  - 新增模块级 `_can_bind(host, port)` 与 `is_port_in_use(host, port)`。
- 独立服务端 `F_HamLog_Remote_Log_Server_2.0.0/remote_server.py` 同步（两份一致）。
- 新增 `test_default_port_smoke.py`（18 项全通过）。

### 关键坑：Windows 上「探测端口是否被占用」的正确写法
排查过程逐层否决了三种错误做法，最终结论（已写进代码注释与 MEMORY.md）：

1. **connect 探测不可用**：`settimeout()` 后 `connect_ex` 对**空闲**端口返回
   `WSAEWOULDBLOCK(10035)`，与占用无法区分（占用实测返回 0）。
2. **探测 socket 绝不能设 `SO_REUSEADDR`**：Windows 允许在已设置 `SO_REUSEADDR` 的情况下
   bind 到**同一地址已监听的端口**，导致探测恒判「空闲」。实测：
   holder 在 127.0.0.1 时，probe 127.0.0.1 + reuse=True → bind **成功**（漏判）；
   reuse=False → `FAIL(10048)`（正确）。
3. **Windows 绑定语义不对称，必须试探完全相同的地址**：
   - holder `127.0.0.1` → probe `127.0.0.1` FAIL / probe `0.0.0.0` **OK（漏判）**
   - holder `0.0.0.0`   → probe `0.0.0.0` FAIL / probe `127.0.0.1` **OK（漏判）**

   本项目服务端一律以 `0.0.0.0` 监听，故探测口径就是 `0.0.0.0`（`host` 为空/`::` 时归一为 `0.0.0.0`）。

最终实现（bind 探测、不设 SO_REUSEADDR、按同地址）：
```python
def _can_bind(host, port):        # True=可绑定(空闲)
    s = socket.socket(...)        # 注意：不要 setsockopt SO_REUSEADDR
    try: s.bind((host, int(port))); return True
    except OSError: return False
    finally: s.close()

def is_port_in_use(host, port):
    if not port: return False
    bind_host = host if host and host != '::' else '0.0.0.0'
    return not _can_bind(bind_host, port)
```

### 验证
- `test_default_port_smoke.py`：18/18 通过（空闲/占用判定、`port_status`、`port=0` 自动分配、
  8000 被占时回退自动端口、端到端加密加入）。
- 全部烟测套件：**14/14 通过**。
- 两进程真机回归：开放端口 **8000**、密钥文件 `file/remote_rooms/keys/server_8000.fhlkey`、
  指纹 `CDE8-1906-92F9-4520-5C23-3F83-9F4F-EDB3`；客户端 `encrypted=true`、指纹 `match`；
  双方最终 `file_len=4`（双向同步正常）。
- 编译：`project.py` / `remote_server.py` / `main.py` 及独立服务端 2 文件全部 `py_compile` 通过。

---

## 长期密钥存放位置分离：内嵌→file/、独立→自己目录

用户需求：「直接存到 file 中。服务端存到自己的文件夹」。此前内嵌服务端（「开放多人日志」）
把密钥放在 `file/remote_rooms/keys/`（跟着 fhl_path 走），现明确分离：

| 场景 | 密钥位置 | 由谁指定 |
|---|---|---|
| 内嵌服务端（客户端开房） | `file/keys/server_<端口>.fhlkey` | `project._open_lan()` 传 `key_dir='file'` |
| 独立服务端（`F_HamLog_Remote_Log_Server_2.0.0`） | `<程序目录>/keys/server_<端口>.fhlkey` | `main.py` 传 `key_dir=_app_dir()` |

### 改动
- `remote_server.py`：
  - `_data_dir(fhl_path=None, key_dir=None)` 新增 `key_dir` 形参，优先级
    **显式 key_dir > 日志文件所在目录 > cwd**。
  - `LogServer.__init__` 新增 `key_dir=None` 形参与 `self.key_dir`；`_load_keys` 与
    `_reload_keys_for_port` 统一改为 `_data_dir(self.fhl_path, self.key_dir)`。
- `project.py`：`_open_lan()` 两次 `LogServer(...)`（8000 尝试 + `port=0` 回退）均传 `key_dir='file'`。
- 独立服务端 `main.py`：`LogServer(...)` 显式传 `key_dir=_app_dir()`（此前依赖默认行为，
  虽已等价但语义不明确；现已写明并加注释强调两套密钥相互独立、换端口即换身份）。
- 独立服务端 `remote_server.py` 同步（两份 md5 一致：`c4ccb34c692c508001f4073fa1091ebf`）。
- `.gitignore`：新增 `file/keys/`、`F_HamLog_Remote_Log_Server_2.0.0/keys/`、`file/_key_backup_*/`。

### 验证
- 新增 `__keydir_smoke.py`：**16/16 通过**（`_data_dir` 三级优先级；`key_dir` 落盘；
  内嵌→`file/keys/`；独立→`<app_dir>/keys/`；两套目录互不干扰）。
- 新增 `__keydir_real.py` 实机回归：**16/16 通过**。真实 socket 完整加密握手，
  端口 8000、指纹 `324F-...`、`file/keys/server_8000.fhlkey` 已落盘、
  旧位置 `remote_rooms/keys/` **未被创建**、信任库首次核对 `new` → 记住后 `match`。
- 既有套件回归：`test_remote_smoke` / `test_remote_encrypt_smoke`(25) /
  `test_remote_lifecycle_smoke`(15) / `test_remote_dirty_smoke` / `test_default_port_smoke`(18)
  **全部通过，零回归**。
- 清理：6 个孤立测试密钥（root `keys/`）与测试随机端口信任条目已备份到
  `file/_key_backup_20260919_1333xx/` 后移除；信任库只保留真实使用的 `127.0.0.1:8000`。

### 注意
- 「换端口即换身份」在下述意义上依然成立，但**内嵌与独立服务端即使端口相同也是两把不同的密钥**
  （文件路径不同），因此各自指纹不同、信任库需分别记录，不可混用。
- 备份目录 `file/_key_backup_*/` 属手工产物，确认无用后可删。

---

## 自选卫星数量上限 500（超限拦阻 + 一键清除）

用户需求：「打开卫星过境或选择卫星时，若用户选择了超过 500 颗卫星，则提示"选择的卫星过多"，
并指引重新选择或者一键清除所有选择的卫星（进行二次确认）」。

### 改动
- `satellite_pred.py`：新增 `MAX_SELECTED_SATELLITES = 500` 与
  `clamp_selected_count(n, limit)` → 返回 `(保留集合, 被裁掉数量)`。裁剪取**排序后前 N 个**，
  保证同一输入集合每次结果一致（set 无序，否则重开对话框选择会莫名变化）。
- `satellite_window.py` `SatelliteSelectDialog`：
  - 重写 `accept()`：勾选数 > 500 时不调 `super().accept()`，弹出 `QMessageBox`（标题
    「选择的卫星过多」），三个按钮：**重新选择**（AcceptRole，留在对话框）/ **一键清除所有选择**
    （DestructiveRole）/ 取消。点「一键清除」后再弹 `QMessageBox.question`（标题「确认清除」，
    回答 No/Yes）做**二次确认**，选 Yes 才 `_clear_all_selected()`（对全部卫星生效，不只可见项）。
    「重新选择」与「取消」都只是留在对话框，不做任何修改。
  - 新增 `_clear_all_selected()`；新增 `_refresh_selected_count()`。
  - `_filter` 末尾刷新计数标签，显示 `…　已选 N 颗`；超限时整条转红色并追加「，超过上限 500 颗」。
  - **踩坑（已修复的真实 bug）**：`_refresh_selected_count` 初版读 `self.count_label.text()`
    当基数再追加，导致每次勾选变化都**累加**成一大串（实测输出几百个"已选 N 颗"）。
    改为把基数文本缓存在 `self._count_base` / `self._count_base_red`，每次由缓存**重建**标签。
- 调用方（`satellite_window.open_select` / `mutual_window.open_select`）在 `Accepted` 后
  再 `sp.clamp_selected_count(...)` 兜底一次。
- 加载时兜底：`satellite_window` / `mutual_window` 读 `sat_sats` / `mu_sats` 后立即裁剪，
  避免旧设置里的历史大规模选择一打开窗口就卡住。
- `import_tle()` 导入后默认全选改为「全选但受上限约束」，超量时提示只选了前 500 颗。

### 验证
- 新增 `test_max_selected_smoke.py`：**24/24 通过**（常量/裁剪确定性/未超限正常关闭/
  超限拦阻文案与按钮/「重新选择」不改选择/清除的二次确认选否不清空、选是清空/清空后标签转正常）。
- 既有对话框测试回归：`__pin_selected_smoke.py`(6 项) / `__rowhidden_smoke.py` /
  `__rowhidden_cmp.py` 全部通过，计数标签新增的「已选 N 颗」后缀不与原有用例冲突。
- `py_compile`：`satellite_pred.py` / `satellite_window.py` / `mutual_window.py` 通过。

### 离屏测试新踩的坑（重要）
在 offscreen 下测试**嵌套模态消息框**（`QMessageBox.exec()` 由 `accept()` 内部弹出）时：
1. **链式 `QTimer.singleShot` 不可用** —— 模态嵌套后不再重新触发，永远点不到按钮、测试挂死。
   必须用**独立的 `QTimer()` 对象**（`setInterval(50)` + `timeout.connect`）轮询点击。
2. 查找按钮**必须限定 `isinstance(w, QMessageBox) and w.isVisible()`**：`topLevelWidgets()`
   会同时返回 `SatelliteSelectDialog` 自身，不加限定会点到隐藏控件（不触发槽函数→挂死）。
   需要点主对话框的按钮（如「取消」）时，再单独放开"可见的非 QMessageBox QDialog"。
3. **必须显式触发 `dlg.accept()`**（`QTimer.singleShot(150, dlg.accept)`），否则消息框根本不出现。
4. 「重新选择」后对话框**故意保持打开**，测试必须再补一次点击（如「取消」）结束 `exec()`，
   否则永久阻塞。抓取二次确认框要用 `delay_ms` 跳过前面那个超限提示框。

---

## 超限提示措辞调整 + 「清除所有选择」改为重开新窗口（提高性能）

用户两次追加要求：
1. 「不要显示"一键"这两个字」
2. 「清除所有选择后：直接弹出新的选择窗口，提高性能」

### 改动
- **去掉"一键"**：按钮文本 `一键清除所有选择` → **`清除所有选择`**；超限提示正文里的
  "或一键清除全部已选卫星" → "或清除全部已选卫星"；两处 docstring 一并改，不留"一键"字样。
  测试文件 `test_max_selected_smoke.py` 的断言/点击器/注释同步。
- **清除后重开新窗口**（性能）：
  - `SatelliteSelectDialog.__init__` 新增 `self.reset_requested = False` 标志。
  - `accept()` 里选「清除所有选择」并二次确认 Yes 后：**不再原地 `_clear_all_selected()` 逐项重置**，
    而是 `self.reset_requested = True` + `super().reject()` 直接关闭，交调用方重建新窗口。
    （原地重置对 900+ 项要逐次 `setCheckState` → 每次触发 `itemChanged` → 重排 + 刷新标签，
    整体 O(N²) 级卡顿；重建则构造期一次完成，成本等价于零。）
  - **删除 `_clear_all_selected()`**（已无调用方）。
  - 二次确认文案补充「并重新打开选择窗口」。
  - 两个调用方（`satellite_window.open_select` / `mutual_window.open_select`）改为
    `while True:` 循环：`Accepted` → 裁剪+break；`Rejected` 且 `reset_requested` → 置空选择后
    继续循环重开；否则 `return`（用户取消）。`_persist()/run_prediction()/推送地图` 移出循环，
    只在最终确定后执行一次。
  - **关键**：`reset_requested` 必须在循环里调用方清除后（`selected_names = set()`）再重开，
    否则新窗口会继承旧的 900 颗（测试第 5b 节专门验证新窗口 `已选 0`）。

### 验证
- `test_max_selected_smoke.py` 扩到 **29/29 通过**：新增「重新选择不触发重开」、
  「二次确认文案说明会重开」、「选是→Rejected 且 reset_requested=True」、
  以及**重开循环模拟**（首轮超限→清除→请求重开；次轮新窗口为空选择、可正常确定返回）。
- 既有对话框回归：`__pin_selected_smoke.py`(6 项) / `__rowhidden_smoke.py` /
  `__rowhidden_cmp.py` 全部通过。
- `py_compile`：`satellite_window.py` / `mutual_window.py` 通过。

---

## m_xml 中保存的自选卫星超限时，同样弹提示

用户需求：「若 m_xml 中选择了超过 500 个，也像这样提示」。
（此前是**静默裁剪**到 500，用户看不到任何说明。）

### 改动
- 抽出共用函数 `satellite_window.prompt_over_limit_selection(parent, n, limit, source_hint='')`
  → 返回 True 表示用户确认「清除所有选择」（已过二次确认），False 表示「重新选择」/「取消」。
  超限提示框与二次确认框的文案/按钮全部集中在此，`SatelliteSelectDialog.accept()` 改为调用它，
  避免两处各写一遍而走样。
  `source_hint` 用于附上来源说明（设置文件路径），让用户知道提示从何而来。
- 打开窗口时**不再静默裁剪**：`satellite_window.main()` / `mutual_window.main()` 里读
  `sat_sats` / `mu_sats` 后仍先 `clamp_selected_count` 兜底（保证不卡），但记下原始数量
  `_oversized_from_settings`；在 `win.show()` 之后弹 `prompt_over_limit_selection(...)`，
  选「清除所有选择」则 `selected_names.clear()` + `_persist()`。
- 提示正文示例：「设置文件 file/m_xml.txt 中保存的自选卫星已超过上限。\n
  已勾选 1200 颗，单次最多支持 500 颗。\n请减少勾选后再确定，或清除全部已选卫星重新挑选。」

### 验证
- `test_max_selected_smoke.py` 扩到 **36/36 通过**：新增 7a/7b/7c 三节直接驱动
  `prompt_over_limit_selection`，覆盖「重新选择」不清空、`informativeText` 含 m_xml.txt
  来源说明、二次确认选否/选是。
- 实机验证（`satellite_window.main()` 真开窗）：m_xml 写入 1200 颗 → 打开即弹提示、
  标题/正文/按钮全对、二次确认 Yes 后 `sat_sats` 落盘为 0 —— **6/6 通过**。
- 既有对话框回归：`__pin_selected_smoke.py` / `__rowhidden_smoke.py` / `__rowhidden_cmp.py`
  全部通过。
- 测完已还原 `file/m_xml.txt`（原为 500 颗、坐标为 0,0,0）。

### 离屏测「主窗口 + 模态提示」的新坑（重要）
1. **`main()` 里 `win.show()` 后的同步模态框会阻塞主线程** —— 直接在模块顶层调 `main()`
   会让计时器根本没机会跑。必须 `QTimer.singleShot(80, lambda: sw.main(None))` 延迟触发，
   让计时器链先就位，再进入 main() 内部的模态循环。
2. **`QMessageBox` 标准按钮的 `text()` 带助记符**：实测是 `'&Yes'` / `'&No'`，
   按文本精确匹配 `== 'Yes'` **永远点不到**（表现为测试停在二次确认框上直到超时）。
   必须用 `w.button(QMessageBox.StandardButton.Yes)` 按 role 匹配。自定义 `addButton`
   的按钮（如「清除所有选择」）文本无 `&`，可以按文本匹配。
3. m_xml 里 `m_lat/m_lon` 为 0,0 时会先弹「设置观测站」引导框，抢在超限提示前面 ——
   实机测试前先把坐标写成非零值（如 39.9/116.4/50.0），测完还原。

---

## GitHub Actions 发布全流程（Nuitka → 安装包 → 兼容版 → Releases）

用户需求：把原来的「只打包并上传 artifact」工作流，优化成一次跑完发布全流程 ——
① 打包 Python（保留缓存加速下次）；② Inno Setup 6 生成安装包（自动注入版本号，
文件名形如 `F HamLog 2.4 setup.exe`，代码参考 `F HamLog 2.iss`）；③ 生成兼容版压缩包；
④ 发布到 Releases（tag = 手动输入的版本号；附件 = 正常安装包 + 兼容版压缩包 +
对应版本的远程日志服务端 `F_HamLog_Remote_Log_Server_2.0.0.exe`，无需再次打包）。
后续补充：**Releases 说明内容留空，由作者手动填写**。

### 改动文件
- `.github/workflows/main.yml`（重写，15 步）：`workflow_dispatch` 输入收敛为
  `version`(必填) / `python_version` / `app_exe_name` / `remote_server_exe` / `draft` /
  `upload_debug_artifact`；`permissions: contents: write`；全步骤 `shell: pwsh`。
- `.github/inno/F HamLog 2.iss`（新增，CI 专用）：安装/升级逻辑与
  `F HamLog 2 Inno Setup\F HamLog 2.iss` 完全一致（**AppId 相同**，升级关系不变），
  区别只在变量化 —— 路径全部由 `RepoRoot`（`/D` 注入）推导，不再写死 `D:\...`；
  版本/包名/ExeName 由 `/DMyAppVersion` `/DOutputBaseFilename` `/DMyAppExeName` 注入。
- `.github/inno/ChineseSimplified.isl` + `.gitattributes`（新增）：**GitHub 运行器的
  Inno Setup 6.7.1 不带非官方中文语言包**，故把本机（Inno 6.7.3）已验证可用的语言文件
  内置进仓库，并用 `.gitattributes` 锁定 CRLF（下载来的 kira 版是 LF，且部分消息多加了
  句号，与文件头「不要加句号」的说明相悖，故采用本机版本）。
- `.gitignore`：新增 `/release/`、`/release_assets/`、`/.venv/`、`main.build/`。

### 版本号与命名规则
- 只填 `version`（如 `2.4.0`）→ `display = version -replace '\.0$',''` 得 `2.4`
  （2.4.1 这类补丁版本保持原样，避免同名覆盖）。
- 安装包 `F HamLog 2.4 setup.exe`；兼容版 `F HamLog 2.4兼容版.zip`；
  Release 附件沿用历史点号风格 `F.HamLog.2.4.setup.exe` / `F.HamLog.2.4.zip`
  （历史 Release 资产就是空格→点号），远程服务端保持原名。
- 安装包**内部 AppVersion 仍用完整版本 2.4.0**（与作者本地 iss 的习惯一致），
  仅文件名取 2.4 —— 用户确认过「版本号只有开始界面有，比如 2.4.0，但安装包名要 2.4」。
- Release：`gh release create <tag> <资产…> --target <sha>`，**不传** `--title`/`--notes`/
  `--generate-notes` → 标题即 tag、正文为空；已存在时只 `gh release upload --clobber`
  覆盖附件，**不 edit**（避免抹掉手写说明）。

### 缓存策略（加速下次打包）
`~\AppData\Local\pip\Cache`、`~\AppData\Local\Nuitka`（辅助工具）、
`.venv`（按 `第三方模块.txt` 哈希；命中后先用 `import` 校验齐全，Python 补丁版本变化导致
venv 失效则自动重建）、`main.build`（按 `*.py` 哈希，源码未变的模块跳过 C 编译与链接）。

### 排查到的 5 个坑（都已修）
1. **PowerShell 里紧跟中文的变量必须写 `${var}`**：`"F HamLog $display兼容版.zip"`
   被解析成变量名 `$display兼容版`（中文属 Unicode 字母），值静默变成空串，
   产物名退化为 `F HamLog .zip`。改用 `${display}` 后正常。
2. **`VersionInfo.ProductVersion` 由系统补尾部空格**：`"2.4.0"` 与 `"2.4.0     "`
   不相等，版本校验必然误报失败 → 必须 `.Trim()` 后比较。
3. `GITHUB_ENV` / 步骤摘要统一改用 `[System.IO.File]::AppendAllLines` +
   `UTF8Encoding($false)`，避开 `Out-File -Encoding utf8` 在 PS5.1（带 BOM）与
   pwsh（不带 BOM）下的差异。
4. 多行文本数组**不要用 `[string[]]` 强转**：嵌套数组元素会被拼成空格分隔的一行
   （步骤摘要的 3 个附件挤成一行），改用 `List[string]` 逐行 `Add` 后 `.ToArray()`。
5. 7-Zip 打包要 `Push-Location main.dist; 7z a -tzip <dst> *`，条目才落在压缩包根部
   （与历史兼容版结构一致）；无 7z 时退回 `Compress-Archive`。

### 本地验证手法（无 gh、无 CI 也能测）
1. 用 PyYAML 解析工作流 → 抽出每段 `run` → 替换 `${{ }}` 占位 → 交
   `[System.Management.Automation.Language.Parser]::ParseFile` 查语法（8/8 通过）。
2. 建一个桩 `gh.cmd`（输出参数并可按 `GH_VIEW_EXIT` 返回 0/1），前置到 `PATH`，
   真实跑「解析版本号 / 生成安装包 / 生成压缩包 / 准备附件 / 发布」各步，校验路径、
   gh 参数拼装、两个分支（tag 存在/不存在）与步骤摘要格式。
3. 实测产物：安装包 `release\F HamLog 2.4 setup.exe`（68.4→65.2 MB，ProductVersion=2.4.0，
   ISCC 约 70 秒）；兼容版 `release\F HamLog 2.4兼容版.zip`（101400416 字节，
   与本地手工打的 2.4 兼容版压缩包字节数完全一致）；`release_assets` 三个附件齐全
   （65.2 / 96.7 / 81.5 MB）。
4. 本机工具链：Inno Setup 6.7.3（`%LOCALAPPDATA%\Programs\Inno Setup 6\ISCC.exe`）、
   7-Zip 26.02。

