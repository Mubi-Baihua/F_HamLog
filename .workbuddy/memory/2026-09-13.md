- 将 main.py 主页的「QRZ主页」按钮改造为「加入多人日志」：网格(0,1)位置的 button_qrz 改为调用 join_server；删除下方独立的重复 button_join 入口；删除已无引用的 qrz_page 函数及 webbrowser/urllib.parse 导入。py_compile 通过。
- 修改 mutual_window.py：通联预测窗口 TLE 载入后不再自动弹出「选择卫星」对话框（移除 on_fetched 中的 open_select() 自动调用）；selected_names 为 None 时仅置为空集合，由 run_prediction 在状态栏提示用户点击「选择卫星…」。卫星过境窗口(satellite_window.py)的 auto_prompt_pending 自动弹窗逻辑保留不变。py_compile 通过。
- 修改 mutual_window.py：移除通联预测窗口中所有「台站 A（本站）尚未设置位置」的提示——① run_prediction 中 va 为 (0,0) 时改为静默 return（不再弹状态栏提示）；② 窗口打开时不再 QMessageBox.information 弹「未设置本站位置」对话框。台站 B（对方）未设置的提示保留。卫星过境窗口仍负责引导本站位置设置。py_compile 通过。
- 新增 main.py：启动时自动清理 file/remote_rooms 中超过 3 天的多人日志房间文件。实现：模块级 _cleanup_remote_rooms(days=3)，在 main() 开头调用。判定优先用文件名时间戳「<前缀>_YYYYMMDD_HHMMSS.fhl」（正则 (\d{8})_(\d{6})），解析失败回退文件 mtime；仅删 .fhl，目录不存在直接返回。py_compile 通过，并用临时夹具做了功能测试（旧/未来/近期/mtime回退/非.fhl 五类用例断言通过）。
- 教训（重要）：file/remote_rooms 受 git 跟踪！本轮验证脚本的清理循环误用 os.remove 遍历删空整个目录，删掉了 26 个已跟踪房间文件（多人日志_*/房间_*）。已用 git checkout HEAD -- file/remote_rooms/ 全部恢复。后续：在该目录做测试只能用「针对性删除指定夹具」或移到回收站，绝不可 os.listdir 后整体 remove；且测试结束只删自己创建的夹具。
- 修复 batch_backup 错误扩展名：备份常量 BATCH_BACKUP 由 batch_backup.fh 改回 batch_backup.fhl（bug：旧版写成 .fh 而非 .fhl）。backup.py 新增导入期一次性迁移：旧 .fh 存在时 rename 到 .fhl（用 os.replace 而非 os.rename——Windows 下 rename 不能覆盖已存在目标文件，且原 except OSError 会吞掉该错误导致迁移静默失效）；旧 .fh 为空则直接删除。batch_project.py / main.py 均通过 backup.BATCH_BACKUP 引用，无需改动。已用 venv(含 cryptography) 做功能测试：制造非空 .fh + 空 .fhl，导入 backup 后 .fh 消失、内容落入 .fhl（21 字节），断言通过；测试夹具已清理恢复。
- 优化批量记录「完成」页界面(batch_project.py)：原 700x140 固定窗 + 4 按钮单行拥挤。改为：标题「批量记录完成」(蓝色粗体) + 摘要「已成功录入 N 条记录，请选择保存方式：」+ 4 个操作按钮 2x2 网格(QGridLayout，列均分、按钮高 38) + 底部右对齐「取消」(宽 96)。窗口改 setMinimumWidth(460) 不再固定死高。py_compile 通过。
