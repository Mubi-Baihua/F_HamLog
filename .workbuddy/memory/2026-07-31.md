# 2026-07-31

## 屏蔽大文件并推送到 GitHub
- 任务：把 `F HamLog 2.1/F HamLog 2.1 setup.exe`（65MB）等大文件挡在 GitHub 之外并推送。
- 发现：`.gitignore` 原有笔误 `.F HamLog 2.1/...`（多了前导点），与真实目录 `F HamLog 2.1/` 不匹配，规则失效。
- 3 个未推送提交里混入了构建产物：`main.dist/`（263MB，PyInstaller 构建目录，无源码 .py）、`setup.exe`（65MB）、`__pycache__/`（19 个 .pyc）。GitHub 单文件上限 100MB，127MB 的 `main.dist/F HamLog 2.1.exe` 会直接导致 push 失败。
- 处理：保留备份分支 `backup/develop-before-cleanup`（指向原 HEAD 8bfd490）；`git reset --soft origin/develop` 后将上述构建产物 `git reset HEAD --` 取消暂存；修正 `.gitignore`（忽略 `main.dist/`、`F HamLog 2.1/F HamLog 2.1 setup.exe`、`__pycache__/`、`*.pyc`，保留 `F HamLog 2.1.iss` 安装脚本）；压成单个干净提交 `882e269` 后 `git push origin develop` 成功。
- 验证：`git ls-remote origin develop` 返回 `882e269…`，与本地 HEAD 一致；远程树无 `main.dist/setup.exe/__pycache__`。

## 本沙箱推送 GitHub 的环境要点（可复用）
- 走 HTTPS，网络有 MITM 代理（证书签发者 BeyondDimension/SteamTools），系统证书库由 Schannel 信任、OpenSSL 后端会报 "unable to get local issuer certificate"。
- 推送必须用：`git -c http.sslBackend=schannel -c http.schannelCheckRevoke=false -c credential.helper=wincred push origin develop`（禁用吊销检查，否则报 CRYPT_E_NO_REVOCATION_CHECK；凭证从 Windows 凭据管理器取，用户名 Mubi-Baihua）。
- 已设全局 `http.schannelCheckRevoke=false`（保留，便于后续推送；`http.sslBackend` 已还原为默认 schannel）。
- 注意：本沙箱 `refs/remotes/origin/*` 松散引用/写入 packed-refs 均不持久，`git status` 可能显示 `develop...origin/develop [gone]`，但 `ls-remote` 确认远程正常，不影响 push。
